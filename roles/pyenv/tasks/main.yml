- git:
    repo: 'https://github.com/pyenv/pyenv.git'
    dest: ~/.pyenv
    # update: no

- name : Install deb packages
  apt:
    name:
      - build-essential
      - curl
      - libssl-dev
      - libxml2-dev
      - libxmlsec1-dev
      - libffi-dev
      - liblzma-dev
      - libbz2-dev
      - libreadline-dev
      - libsqlite3-dev
      - libncurses5-dev
      - libncursesw5-dev
      - llvm
      - make
      - python-openssl
      - tk-dev
      - wget
      - xz-utils
      - zlib1g-dev
  become: yes